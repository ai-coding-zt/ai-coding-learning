# AI Coding Skills 完全指南

!!! info "开放标准"
    Skills 是由 Anthropic 在 2025 年末发布的开放标准，用于创建可重用的 AI 指令包。

## 🎯 什么是 Skills？

**Skills（技能）** 是可重用的提示词包，可以包含可选的捆绑资源（脚本、引用、资产），用于教授 AI 代理执行特定任务。它们将通用代理转变为具有领域特定知识和工作流的专业代理。

### 核心特点

| 特性 | 描述 |
|------|------|
| **格式** | 带有 YAML 前言的 Markdown 文件 |
| **结构** | 基于目录，`SKILL.md` 作为入口点 |
| **加载** | 渐进式披露（元数据 → 指令 → 资源）|
| **范围** | 可在项目和团队间重用 |
| **分发** | Git 仓库、插件或 CLI 安装 |

### Skills vs MCP 服务器

| 方面 | Skills | MCP 服务器 |
|------|--------|------------|
| **复杂度** | 简单（Markdown + 可选脚本）| 复杂（需要服务器实现）|
| **上下文使用** | 最小（~100 tokens 用于元数据）| 较高（完整工具架构）|
| **加载** | 懒加载（按需）| 急加载（连接时）|
| **用例** | 工作流、领域知识、提示词 | 外部 API 集成、复杂工具 |
| **设置** | 即插即用（复制文件夹）| 需要服务器进程 |
| **网络** | 本地/可选 | 通常需要网络 |

!!! tip "最佳实践"
    使用 **Skills** 处理可重用的工作流和提示词；使用 **MCP** 处理外部 API 集成和复杂工具。

## 🏗️ 架构与实现

### 目录结构

```
my-skill/
├── SKILL.md              # 必需 - 主要指令和元数据
├── scripts/              # 可选 - 可执行工具
│   └── helper.py
├── references/           # 可选 - 按需加载的文档
│   └── api-docs.md
└── assets/               # 可选 - 模板、图片、静态文件
    └── template.html
```

### SKILL.md 格式

每个技能需要一个带有 YAML 前言的 `SKILL.md` 文件：

```markdown title="SKILL.md"
---
name: code-reviewer
description: >
  审查代码的最佳实践、安全问题和风格一致性。
  当被要求审查代码、进行代码评审或检查问题时使用。
  支持 Python、JavaScript、TypeScript 和 Go。
license: MIT
metadata:
  author: your-name
  version: "1.0.0"
---

# Code Reviewer Skill

## 使用时机
- 触发条件 1
- 触发条件 2

## 快速参考
[表格、常用命令]

## 指令
1. 第一步
2. 第二步

## 详细文档
详见 [references/](references/) 获取完整文档。
```

### 前言规范

| 字段 | 必需 | 描述 |
|------|------|------|
| `name` | ✅ 是 | 技能标识符（小写、连字符，最多 64 字符）|
| `description` | ✅ 是 | 技能功能和何时使用（最多 1024 字符）|
| `license` | ❌ 否 | 许可证名称或文件引用 |
| `compatibility` | ❌ 否 | 环境要求（最多 500 字符）|
| `metadata` | ❌ 否 | 自定义数据的键值对 |
| `allowed-tools` | ❌ 否 | 预批准工具（实验性）|

### 渐进式披露系统

```
级别 1: 元数据（~100 tokens）
├── name
└── description
    ↓（启动时加载所有技能）

级别 2: SKILL.md 正文（推荐 < 5000 tokens）
├── Instructions
├── Examples
└── Guidelines
    ↓（技能激活时加载）

级别 3: 捆绑资源（无限制）
├── scripts/     → 执行时不加载到上下文
├── references/  → 仅在引用时加载
└── assets/      → 在输出中使用而不加载
```

## 🚀 创建自定义技能

### 步骤 1：初始化技能目录

```bash
mkdir -p my-skill
cd my-skill
touch SKILL.md
```

### 步骤 2：创建 SKILL.md

```markdown title="SKILL.md"
---
name: api-tester
description: >
  全面的 API 测试工作流。当用户提到：
  "测试 API"、"端点测试"、"API 验证" 或处理
  REST/GraphQL 端点时使用。
---

# API Tester Skill

## 检查清单

### 基础验证
- [ ] 端点可访问性
- [ ] 认证机制
- [ ] 请求格式
- [ ] 响应格式

### 性能测试
- [ ] 响应时间
- [ ] 并发处理
- [ ] 负载测试

### 安全测试
- [ ] 输入验证
- [ ] SQL 注入
- [ ] XSS 防护
- [ ] 认证绕过

## 快速参考

| 方法 | 用途 | 示例 |
|------|------|------|
| GET | 获取资源 | `/api/users` |
| POST | 创建资源 | `/api/users` |
| PUT | 更新资源 | `/api/users/1` |
| DELETE | 删除资源 | `/api/users/1` |

## 使用方法

1. 识别要测试的 API 端点
2. 运行 `scripts/test-endpoint.sh` 进行自动化测试
3. 应用上述检查清单
4. 提供带有严重级别的结构化反馈
```

### 步骤 3：添加支持资源

```bash title="scripts/test-endpoint.sh"
#!/bin/bash

ENDPOINT=$1
METHOD=${2:-GET}

case "$METHOD" in
  GET)
    curl -s -w "\nHTTP Status: %{http_code}\n" "$ENDPOINT"
    ;;
  POST)
    curl -s -X POST -H "Content-Type: application/json" \
      -w "\nHTTP Status: %{http_code}\n" "$ENDPOINT"
    ;;
  *)
    echo "Unsupported method: $METHOD"
    exit 1
    ;;
esac
```

### 步骤 4：安装技能

=== "Claude Code"
    ```bash
    # 用户级别（所有项目可用）
    git clone https://github.com/yourusername/my-skill ~/.claude/skills/my-skill
    
    # 或项目级别
    git clone https://github.com/yourusername/my-skill ./.claude/skills/my-skill
    ```

=== "OpenCode"
    ```bash
    # 原生支持（v1.0.190+）
    mkdir -p skill
    cp -r my-skill skill/
    
    # 配置 opencode.json
    # { "permission": { "skill": { "my-skill": "allow" } } }
    ```

## 📚 官方技能

| 技能 | 用途 | 位置 |
|------|------|------|
| **docx** | Word 文档创建/编辑 | `anthropics/skills/skills/docx` |
| **pdf** | PDF 处理 | `anthropics/skills/skills/pdf` |
| **pptx** | PowerPoint 创建 | `anthropics/skills/skills/pptx` |
| **xlsx** | Excel 电子表格处理 | `anthropics/skills/skills/xlsx` |
| **skill-creator** | 构建技能的元技能 | `anthropics/skills/skills/skill-creator` |

## 🎯 使用场景

### 开发与技术

- **代码审查工作流**
- **测试模式**（单元、集成、E2E）
- **Git 操作**（PR 创建、提交规范）
- **文档生成**
- **API 客户端生成**

### 企业与沟通

- **品牌指南执行**
- **文档模板**（提案、报告）
- **邮件工作流**（起草、格式化）
- **会议记录模板**

### 数据与分析

- **数据处理管道**
- **图表/可视化生成**
- **报告自动化**
- **数据库查询模式**

## 💡 最佳实践

### 1. 保持简洁

- 默认假设：Claude 已经非常聪明
- 只添加 Claude 没有的上下文
- 质疑每个段落："这是否值得它的 token 成本？"
- 保持在 500 行以下

### 2. 设置适当的自由度

| 自由度级别 | 使用时机 | 格式 |
|------------|----------|------|
| **高** | 多种有效方法、上下文相关决策 | 基于文本的指令 |
| **中** | 存在首选模式、有些变化可接受 | 伪代码或带参数的脚本 |
| **低** | 脆弱操作、一致性关键 | 带少量参数的特定脚本 |

### 3. 使用触发感知描述

```yaml
description: >
  全面的 API 测试工作流。当用户提到以下内容时使用：
  "测试 API"、"端点测试"、"API 验证" 或处理
  REST/GraphQL 端点。
```

### 4. 渐进式披露模式

**模式 1：高级指南 + 引用**
```markdown
## 快速开始
[基本指令]

## 高级功能
- **表单填写**: 详见 [FORMS.md](references/FORMS.md)
- **API 参考**: 详见 [REFERENCE.md](references/REFERENCE.md)
```

**模式 2：按领域组织**
```
bigquery-skill/
├── SKILL.md（概览）
└── references/
    ├── finance.md
    ├── sales.md
    └── marketing.md
```

## 🔗 安装位置

| 工具 | 用户级别路径 | 项目级别路径 |
|------|--------------|--------------|
| **Claude Code** | `~/.claude/skills/` | `.claude/skills/` |
| **OpenCode** | `~/.config/opencode/skill/` | `skill/` |
| **Codex** | `~/.codex/skills/` | `.codex/skills/` |

## 📖 学习资源

### 官方文档

| 资源 | URL |
|------|-----|
| **技能规范** | https://agentskills.io/specification |
| **集成指南** | https://agentskills.io/integrate-skills |
| **什么是技能** | https://agentskills.io/what-are-skills |
| **Claude 技能文档** | https://platform.claude.com/docs/en/agents-and-tools/agent-skills/overview |

### GitHub 仓库

| 仓库 | 描述 |
|------|------|
| [官方技能](https://github.com/anthropics/skills) | Anthropic 的参考实现 |
| [Better Skill Builder](https://github.com/gmickel/better-skill-builder) | 生产模式 |
| [Skills Reference Library](https://github.com/agentskills/agentskills) | Python 工具 |

### 教程

| 资源 | 类型 |
|------|------|
| [技能编写最佳实践](https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices) | 文档 |
| [构建技能完整指南](https://resources.anthropic.com/hubfs/The-Complete-Guide-to-Building-Skill-for-Claude.pdf) | PDF |
| [OpenCode 技能教程](https://brave2049.com/groups/artificial-intelligence-learning/forum/discussion/zhu-quan-ge-ren-bi-kan-de-opencode-jiao-cheng-zhi-di-si-ke-2026-ban/) | 视频 |

---

## 下一步

- 🛠️ [创建你的第一个技能](./creating-skills.md)
- 📋 [技能生命周期管理](./lifecycle.md)
- 📚 [浏览技能示例](./examples/)
